// Function for divide
const divide = function(a, b) {
    return a / b;
  };
  
  // Function for square
  const square = x => x * x;
  
  // Function for add
  const add = (a, b) => a + b;
  
  console.log(divide(2000, 100)); 
  console.log(square(5)); 
  console.log(add(3, 7)); 
  